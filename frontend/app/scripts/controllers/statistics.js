'use strict';

angular.module('maXTouchStudioPortal')
    .controller('StatisticsCtrl', function ($scope, alert, $state, $http, API_URL) {

      /**  console.log('calling backend')
        $http.get(API_URL + 'feedback')
            .success(function (feedbacks) {                
                $scope.feedback = feedbacks;
            })
            .error(function (err) {
                console.log('Unable to load feedback..!!')
                    //alert('warning', 'Unable to load gallery items', err.message);
            })**/
    });
